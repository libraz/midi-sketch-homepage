<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 960 342" role="img" aria-labelledby="cam-ttl cam-dsc">
  <title id="cam-ttl">Where chord knowledge enters melody writing</title>
  <desc id="cam-dsc">On the left, one candidate melody is written in four steps: a phrase plan, then a pitch chosen note by note with a chord tone scoring sixteen points, a root or fifth four more and a scale tone twelve, then embellishment, then a downbeat constraint that snaps an illegal note onto a chord tone while retaining a licensed accented dissonance that resolves down by step. That whole pass runs up to a hundred times per section. On the right, each candidate is scored, the weaker half is dropped, and one of the survivors is picked by a weighted roll.</desc>

  <defs>
    <marker id="cam-a" markerUnits="userSpaceOnUse" markerWidth="9" markerHeight="9" refX="8" refY="4.5" orient="auto">
      <path class="ah" d="M0,0 L9,4.5 L0,9 z"/>
    </marker>
  </defs>

  <rect class="bg" x="0" y="0" width="960" height="342" rx="12"/>

  <rect class="grp" x="24" y="52" width="496" height="204" rx="8"/>
  <text class="lbl" x="38" y="72">one candidate — written up to 100 times per section</text>

  <rect class="chip" x="38" y="84" width="468" height="34" rx="5"/>
  <text class="hb" x="50" y="106">1 · phrase plan</text>
  <text class="s" x="200" y="106">where phrases begin and end</text>

  <rect class="chip" x="38" y="126" width="468" height="34" rx="5"/>
  <text class="hb" x="50" y="148">2 · pitch per note</text>
  <text class="s" x="200" y="148">chord tone +16, root or 5th +4, scale tone +12</text>

  <rect class="chip" x="38" y="168" width="468" height="34" rx="5"/>
  <text class="hb" x="50" y="190">3 · embellish</text>
  <text class="s" x="200" y="190">passing and neighbour tones</text>

  <rect class="chip" x="38" y="210" width="468" height="34" rx="5"/>
  <text class="hb" x="50" y="232">4 · downbeat check</text>
  <text class="s" x="200" y="232">Keep legal accents; snap other notes</text>

  <path class="ln" d="M 522 154 H 564" marker-end="url(#cam-a)"/>

  <rect class="grp" x="568" y="52" width="368" height="204" rx="8"/>
  <text class="lbl" x="582" y="72">choosing between them</text>

  <rect class="chip" x="582" y="84" width="340" height="48" rx="5"/>
  <text class="hb" x="594" y="104">score each candidate</text>
  <text class="s" x="594" y="122">style 0.4 + culling 0.4 + bias 0.2</text>

  <path class="ln" d="M 752 132 V 146" marker-end="url(#cam-a)"/>

  <rect class="chip" x="582" y="148" width="340" height="34" rx="5"/>
  <text class="s" x="594" y="170">drop the weaker half</text>

  <path class="ln" d="M 752 182 V 196" marker-end="url(#cam-a)"/>

  <rect class="card" x="582" y="198" width="340" height="44" rx="6"/>
  <text class="hb" x="594" y="218">weighted random pick</text>
  <text class="s" x="594" y="234">among the survivors</text>

  <path class="seam" d="M 24 278 H 936"/>
  <text class="s" x="480" y="300" text-anchor="middle">Only beats 1 and 3 are measured for chord-tone fit — the score is the share of those notes landing on a chord tone.</text>
  <text class="s" x="480" y="318" text-anchor="middle">There is no separate refinement pass: the downbeat snap and the candidate cull are where harmony is enforced.</text>
</svg>
